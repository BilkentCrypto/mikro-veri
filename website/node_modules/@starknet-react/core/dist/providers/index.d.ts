import React from 'react';
import { StarknetProviderProps } from './starknet';
export * from './starknet';
export * from './transaction';
/** Arguments for `StarknetConfig`. */
export declare type StarknetConfigProps = Omit<StarknetProviderProps, 'children'> & {
    children: React.ReactNode;
};
/** Root component that manages the state of all starknet-react hooks. */
export declare function StarknetConfig({ children, ...config }: StarknetConfigProps): JSX.Element;
