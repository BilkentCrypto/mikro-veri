"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useNetwork = void 0;
const react_1 = require("react");
const __1 = require("..");
const network_1 = require("../network");
/**
 * Hook for accessing the current connected chain.
 *
 * @remarks
 *
 * The network object contains information about the
 * network, including block explorers.
 *
 * @example
 * This example shows how to display the current network name.
 * ```tsx
 * function Component() {
 *   const { chain } = useNetwork()
 *
 *   return <span>{chain.name}</span>
 * }
 */
function useNetwork() {
    const { library } = (0, __1.useStarknet)();
    const chain = (0, react_1.useMemo)(() => {
        if (!library) {
            return undefined;
        }
        return (0, network_1.chainById)(library.chainId);
    }, [library]);
    return { chain };
}
exports.useNetwork = useNetwork;
//# sourceMappingURL=network.js.map