import { StarknetChainId } from 'starknet/constants';
export declare type BlockExplorerName = 'voyager';
export declare type BlockExplorer = {
    name: string;
    url: string;
};
/** Information about a StarkNet network. */
export declare type Chain = {
    /** Chain ID. */
    id: string;
    /** Human-readable name. */
    name: string;
    /** Block explorer. */
    blockExplorer?: BlockExplorer;
    /** Flag for test networks. */
    testnet?: boolean;
};
/** Returns a chain information from its id. */
export declare function chainById(chainId: StarknetChainId): Chain | undefined;
